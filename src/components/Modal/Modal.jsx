const Modal = ({ bigImageURL }) => {
  <div class="Overlay">
    <div class="Modal">
      <h1 className="test">TEST</h1>
      <img src={bigImageURL} />
    </div>
  </div>;
};

export default Modal;
